let monCanard = "donald";
const nombreDePate = 2;
const nombreDAile = 2;


/* ici les paramètres ne servent à rien car on ne s'en sert pas dans la fonction
 */function canard(nombreDePate,nombreDAile) {
    let maCanette = "daisy";
    let mesCanards = maCanette += monCanard;
    return mesCanards;
};

console.log(canard());

